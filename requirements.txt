transformers>=5.0.0
huggingface_hub>=0.27.0
torch>=2.0.0
accelerate>=0.30.0
soundfile>=0.12.0
scipy>=1.10.0
numpy>=1.24.0

# Optional: Flash Attention 2 (select flash_attention_2 in the loader node)
# Requires a CUDA-capable GPU and a matching CUDA toolkit.
# pip install flash-attn --no-build-isolation
